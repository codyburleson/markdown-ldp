---
rdf: property
id: influenced
domain: [[Work]]
---

X influenced Y — X shaped the development of Y, without necessarily causing it.

Deliberately carries **no alignment**. A vault always contains predicates nobody
has curated yet, and `spec/02` §5.10 rule 4 requires that this keep working:
mint a vault-local IRI, flag it "undefined vocabulary", never error, never block
indexing. This note is what an alignment-detection pass should find first
(`spec/02` §5.7 step 1).
